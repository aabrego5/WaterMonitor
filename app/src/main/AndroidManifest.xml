<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"


    package="com.example.watermonitor">

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        tools:replace="android:icon, android:label"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TrackingPageSinkDay"></activity>
        <activity android:name=".TrackingPageSinkWeek" />
        <activity android:name=".TrackingPageSinkWeeks" />
        <activity android:name=".TrackingPageSinkMonth" />
        <activity android:name=".TrackingPage" />
        <activity android:name=".HomePage" />
        <activity android:name=".LoginPage" />
        <activity android:name=".WelcomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>